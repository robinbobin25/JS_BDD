#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jquery-json/bin/grunt-jquery-json" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jquery-json/bin/grunt-jquery-json" "$@"
  ret=$?
fi
exit $ret
